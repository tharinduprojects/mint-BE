'use strict';

/**
 * trusted-common-logo router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::trusted-common-logo.trusted-common-logo');
