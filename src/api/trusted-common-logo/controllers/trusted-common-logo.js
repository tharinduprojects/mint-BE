'use strict';

/**
 * trusted-common-logo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::trusted-common-logo.trusted-common-logo');
